
export interface Page {
  id: string;
  title: string;
  imageData: string;
}

export interface AgeGroup {
  label: string;
  prompt: string;
}

export interface DrawingStyle {
  label: string;
  prompt: string;
}
